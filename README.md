# Textree

> 파일시스템 위에서 그대로 살아 숨 쉬는, 나만의 로컬 노션.

**Textree**는 Notion의 사용 경험을 모방하되, SaaS가 아니라 **로컬 전용 데스크톱 앱**입니다.
별도의 데이터베이스 없이 **로컬 파일시스템 자체가 유일한 진실의 원천(single source of truth)** 이며,
좌측 트리뷰는 폴더 구조와, 우측 에디터는 마크다운 파일과 **1:1로 양방향 동기화**됩니다.

당신의 노트는 어딘가의 클라우드 테이블에 갇혀 있지 않습니다. 그냥 당신의 디스크 위에, 평범한 `.md` 파일로 놓여 있습니다.

---

## 왜 Textree인가

`textree`는 본래 "낱장을 한데 묶은 종이 다발"을 뜻합니다. 노트(파일)들의 묶음이라는 이 앱의 본질과 그대로 맞닿아 있습니다.
동시에 수학의 **층(textree) 이론** — 국소(local) 데이터를 일관되게 이어 붙여 전역(global) 구조를 만든다는 개념 — 과도 겹칩니다.
파일시스템에 흩어진 국소적인 노트들을, 끊김 없는 하나의 지식 구조로 엮어내는 일. 그게 이 앱이 하는 일입니다.

---

## 핵심 철학

1. **파일시스템이 곧 데이터베이스다.**
   숨겨진 DB, 인덱스 파일, 독점 포맷이 없습니다. 폴더는 폴더고, 노트는 `.md` 파일입니다.
2. **완전한 소유권.**
   앱을 삭제해도 데이터는 그대로 남습니다. VS Code, Obsidian, `vim`, `cat` 무엇으로 열어도 읽힙니다.
3. **오프라인 우선, 프라이버시 우선.**
   네트워크 없이 동작하며, 데이터가 기기를 떠나지 않습니다.
4. **양방향 라이브 동기화.**
   외부 에디터로 파일을 고치면 앱에 즉시 반영되고, 앱에서 고치면 디스크에 즉시 기록됩니다. 충돌 시 **파일시스템이 항상 옳습니다.**
5. **잠금 없음(no lock-in).**
   언제든 폴더째 들고 떠날 수 있습니다.

---

## 파일시스템 매핑 모델

Textree의 모든 동작은 다음 한 가지 규칙으로 환원됩니다. **트리 노드 = 파일시스템 엔트리.**

| Textree 개념          | 파일시스템 대응                          |
| ------------------- | ---------------------------------------- |
| 워크스페이스(Vault) | 사용자가 지정한 루트 폴더                |
| 자식이 없는 노트    | `note.md` 파일                           |
| 자식이 있는 노트    | `note/` 폴더 + 그 안의 `note.md`         |
| 하위 노트           | 부모 폴더 안의 `.md` 파일 / 하위 폴더    |
| 첨부(이미지 등)     | 같은 폴더에 저장 + 마크다운 상대경로 링크 |
| 트리 정렬 순서      | `.textree/order.json` (선택적 메타)        |

### 예시

```
my-vault/
├── 프로젝트.md                 ← 자식 없는 노트 (리프)
├── 일기/                        ← 자식 있는 노트
│   ├── 일기.md                  ← "일기" 노트 본문
│   ├── 2026-06-13.md
│   └── 2026-06-12.md
└── 자료실/
    ├── 자료실.md
    ├── 회의록.md
    └── assets/
        └── diagram.png          ← 회의록.md 에서 ![](assets/diagram.png) 로 참조
```

> **설계 결정 — "폴더 노트(folder note)" 패턴**
> Notion에서는 모든 페이지가 *본문*과 *하위 페이지*를 동시에 가질 수 있습니다.
> 순수 파일시스템에는 "내용도 있고 자식도 있는 노드"가 없으므로,
> Textree는 **`폴더명/폴더명.md`** 규칙으로 이를 표현합니다.
> 리프 노트는 단순 `.md` 파일로 두어 일반 마크다운 도구와 100% 호환을 유지합니다.
> (대안으로 `index.md` 규칙도 가능 — 설정에서 선택 예정.)

---

## 주요 기능

### 1차 목표 (MVP)

- [ ] 워크스페이스(루트 폴더) 열기 / 전환
- [ ] 폴더 ↔ 트리뷰 실시간 양방향 동기화 (파일 워처 기반)
- [ ] 마크다운 에디터 (라이브 프리뷰 또는 WYSIWYG)
- [ ] 노트 생성 / 이름 변경 / 이동 / 삭제 → 즉시 파일시스템 반영
- [ ] 드래그 앤 드롭으로 트리 재배치 (= 파일 이동)
- [ ] 이미지 붙여넣기 → 로컬 저장 + 상대경로 링크 자동 삽입

### 이후

- [ ] 전문 검색 (파일 인덱싱, DB 없이)
- [ ] 위키링크 `[[노트]]` 및 백링크
- [ ] 노트 간 그래프 뷰
- [ ] 슬래시 커맨드 / 블록 기반 편집
- [ ] 테이블, 체크리스트, 코드 블록, 콜아웃 등 Notion식 블록
- [ ] 다크/라이트 테마
- [ ] Git 연동(선택) — 파일 기반이므로 자연스럽게 버전 관리 가능

---

## Notion과의 차이

| 항목            | Notion              | Textree                          |
| --------------- | ------------------- | ------------------------------ |
| 배포 형태       | 클라우드 SaaS       | 로컬 데스크톱 앱               |
| 저장소          | 독점 DB (서버)      | 로컬 파일시스템 (`.md`)        |
| 오프라인        | 제한적              | 완전 지원                      |
| 데이터 소유권   | 벤더 종속           | 100% 사용자 소유               |
| 포맷            | 독점 / export 필요  | 표준 마크다운, 그 자체로 이식 가능 |
| 협업            | 실시간 다중 사용자  | 단일 사용자 (Git 등으로 우회)  |
| 외부 도구 호환  | 낮음                | 모든 마크다운 도구와 호환      |

> Textree는 협업을 포기하는 대신 **소유권·이식성·투명성**을 얻습니다. "나 혼자 쓰는 노션"이라는 명확한 포지셔닝입니다.

---

## 아키텍처 (제안 — 확정 아님)

로컬·경량·파일시스템 중심이라는 요구를 고려한 후보 스택입니다. 확정 전이며 논의 대상입니다.

- **셸:** [Tauri](https://tauri.app) — Rust 백엔드 + 웹 프론트엔드. Electron 대비 경량이며, 파일시스템·파일 워처 접근이 Rust 쪽에서 안전하고 빠름.
- **프론트엔드:** TypeScript + (React 또는 Svelte)
- **에디터 코어:** [CodeMirror 6](https://codemirror.net) 또는 [TipTap/ProseMirror](https://tiptap.dev) (블록 편집을 원하면 후자)
- **파일 감시:** Rust `notify` 크레이트
- **검색:** Rust `tantivy` (로컬 인덱스, 외부 DB 불필요)

> 대안: 풀 .NET 스택을 원하면 .NET MAUI / Avalonia + 자체 마크다운 파서도 가능. 트레이드오프 정리 필요.

---

## 시작하기

> ⚠️ 아직 초기 설계 단계입니다. 빌드/실행 절차는 스택 확정 후 채워집니다.

```bash
# (예정)
git clone https://github.com/iyulab/textree.git
cd textree
# ...
```

---

## 라이선스

MIT

---

*Textree — your notes, on your disk, as plain files.*